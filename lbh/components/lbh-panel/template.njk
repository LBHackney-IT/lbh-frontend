{% from "panel/macro.njk" import govukPanel %}

{% set classes = (params.classes if params.classes else '') + ' lbh-panel' %}

{{ govukPanel({
  titleText: params.titleText,
  titleHtml: params.titleHtml,
  headingLevel: params.headingLevel,
  text: params.text,
  html: params.html,
  classes: classes,
  attributes: params.attributes
})}}
