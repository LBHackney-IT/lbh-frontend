{% from "back-link/macro.njk" import govukBackLink %}

{% set classes = (params.classes if params.classes else '') + ' lbh-back-link' %}

{{ govukBackLink({
  text: params.text,
  html: params.html,
  href: params.href,
  classes: classes,
  attributes: params.attributes
})}}
