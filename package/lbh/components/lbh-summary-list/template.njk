{% from "summary-list/macro.njk" import govukSummaryList %}

{% set classes = (params.classes if params.classes else '') + ' lbh-summary-list' %}

{{ govukSummaryList({
  rows: params.rows,
  classes: classes,
  attributes: attributes
})}}
