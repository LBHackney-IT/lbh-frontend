{% from "tag/macro.njk" import govukTag %}

{% set classes = (params.classes if params.classes else '') + ' lbh-tag' %}

{{ govukTag({
  text: params.text,
  html: params.html,
  classes: classes,
  attributes: params.attributes
})}}
